import { Typography } from '@mui/material';
import Layout from '../../components/layout/Layout';

const PatientMonitorPage = () => {
  return (
    <Layout title="Patient Monitor">
      <Typography variant="h4">Patient Monitor</Typography>
      <Typography>This is where the patient monitoring features will be displayed.</Typography>
    </Layout>
  );
};

export default PatientMonitorPage;

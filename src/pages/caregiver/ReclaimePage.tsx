import { Typography } from '@mui/material';
import Layout from '../../components/layout/Layout';

const ReclaimePage = () => {
  return (
    <Layout title="ReclaiMe™">
      <Typography variant="h4">ReclaiMe™</Typography>
      <Typography>This is where the ReclaiMe™ features will be displayed.</Typography>
    </Layout>
  );
};

export default ReclaimePage;
